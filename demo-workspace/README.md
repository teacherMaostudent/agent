# Controlled Scan Demo Workspace

This directory is the only file tree mounted into the local RAG Query container as the
`workspace` scan scope. It deliberately contains no credentials, customer data, or source
outside this demonstration fixture. Selecting another directory in Agent Desktop only sends a
bounded file manifest; it does not change this server-side permission boundary.
